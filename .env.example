# Copy this file to .env.local and fill in the values you need.

# Required unless you deploy to a Vercel project you've linked with `eve link`
# (in which case Vercel OIDC handles auth for you automatically).
# Get a key at https://vercel.com/docs/ai-gateway
AI_GATEWAY_API_KEY=
